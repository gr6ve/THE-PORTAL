-- Gui to Lua
-- Version: 3.2

-- Instances:

local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local HOODMODDEDPORTAL = Instance.new("TextLabel")
local UIGradient = Instance.new("UIGradient")
local WELCOME = Instance.new("TextLabel")
local UICorner = Instance.new("UICorner")
local LOCK = Instance.new("TextButton")
local UICorner_2 = Instance.new("UICorner")
local AUTOSTOMP = Instance.new("TextButton")
local UICorner_3 = Instance.new("UICorner")
local CREDIT = Instance.new("TextLabel")

--Properties:

ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Frame.BorderSizePixel = 0
Frame.Position = UDim2.new(0.0178217851, 0, 0.68443197, 0)
Frame.Size = UDim2.new(0, 163, 0, 204)

HOODMODDEDPORTAL.Name = "HOOD MODDED PORTAL"
HOODMODDEDPORTAL.Parent = Frame
HOODMODDEDPORTAL.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
HOODMODDEDPORTAL.BorderColor3 = Color3.fromRGB(0, 0, 0)
HOODMODDEDPORTAL.Size = UDim2.new(0, 163, 0, 36)
HOODMODDEDPORTAL.ZIndex = 5
HOODMODDEDPORTAL.Font = Enum.Font.PermanentMarker
HOODMODDEDPORTAL.Text = "GR6VE PORTAL"
HOODMODDEDPORTAL.TextColor3 = Color3.fromRGB(0, 0, 0)
HOODMODDEDPORTAL.TextSize = 29.000
HOODMODDEDPORTAL.TextWrapped = true

UIGradient.Color = ColorSequence.new{ColorSequenceKeypoint.new(0.00, Color3.fromRGB(255, 255, 255)), ColorSequenceKeypoint.new(1.00, Color3.fromRGB(115, 115, 115))}
UIGradient.Parent = HOODMODDEDPORTAL

WELCOME.Name = "WELCOME"
WELCOME.Parent = Frame
WELCOME.BackgroundColor3 = Color3.fromRGB(134, 134, 134)
WELCOME.Position = UDim2.new(0.0565466732, 0, 0.220588237, 0)
WELCOME.Size = UDim2.new(0, 144, 0, 27)
WELCOME.ZIndex = 5
WELCOME.Font = Enum.Font.SourceSans
WELCOME.TextColor3 = Color3.fromRGB(0, 0, 0)
WELCOME.TextSize = 24.000

UICorner.Parent = WELCOME

LOCK.Name = "LOCK"
LOCK.Parent = Frame
LOCK.BackgroundColor3 = Color3.fromRGB(134, 134, 134)
LOCK.Position = UDim2.new(0.172790214, 0, 0.45588237, 0)
LOCK.Size = UDim2.new(0, 105, 0, 31)
LOCK.Font = Enum.Font.SourceSans
LOCK.Text = "GR6VE LOCK"
LOCK.TextColor3 = Color3.fromRGB(0, 0, 0)
LOCK.TextSize = 14.000
LOCK.MouseButton1Down:Connect(function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/gr6ve/GR6VE-DH-/main/GATEWAY",true))();
end)
UICorner_2.Parent = LOCK

AUTOSTOMP.Name = "AUTOSTOMP"
AUTOSTOMP.Parent = Frame
AUTOSTOMP.BackgroundColor3 = Color3.fromRGB(134, 134, 134)
AUTOSTOMP.Position = UDim2.new(0.172790214, 0, 0.666666746, 0)
AUTOSTOMP.Size = UDim2.new(0, 105, 0, 31)
AUTOSTOMP.Font = Enum.Font.SourceSans
AUTOSTOMP.Text = "MOD FLY"
AUTOSTOMP.TextColor3 = Color3.fromRGB(0, 0, 0)
AUTOSTOMP.TextSize = 14.000
AUTOSTOMP.MouseButton1Down:Connect(function()
	loadstring(game:HttpGet('https://raw.githubusercontent.com/22kristina/swag/main/admin_fly'))()
end)
UICorner_3.Parent = AUTOSTOMP

CREDIT.Name = "CREDIT"
CREDIT.Parent = Frame
CREDIT.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
CREDIT.BorderColor3 = Color3.fromRGB(0, 0, 0)
CREDIT.Position = UDim2.new(0.134031385, 0, 0.878062844, 0)
CREDIT.Size = UDim2.new(0, 117, 0, 24)
CREDIT.Font = Enum.Font.SciFi
CREDIT.Text = "belief#4867"
CREDIT.TextColor3 = Color3.fromRGB(255, 255, 255)
CREDIT.TextSize = 14.000

-- Scripts:

local function YKAJM_fake_script() -- WELCOME.LocalScript 
	local script = Instance.new('LocalScript', WELCOME)

	script.Parent.Text = "Welcome ".. game.Players.LocalPlayer.Name
end
coroutine.wrap(YKAJM_fake_script)()
local function KPGNDA_fake_script() -- Frame.LocalScript 
	local script = Instance.new('LocalScript', Frame)

	-- Draggble
	-- Credits: Veky
	
	local UserInputService = game:GetService("UserInputService")
	
	local gui = script.Parent
	
	local dragging
	local dragInput
	local dragStart
	local startPos
	
	local function update(input)
		local delta = input.Position - dragStart
		gui.Position = gui:TweenPosition(UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y), 'Out', 'Linear', 0, true); -- drag speed
	end
	
	gui.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = gui.Position
	
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)
	
	gui.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)
	
	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			update(input)
		end
	end)
end
coroutine.wrap(KPGNDA_fake_script)()
